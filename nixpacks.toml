[phases.setup]
nixPkgs = [
  "nodejs",
  "nodePackages.npm",
  "python3",
  "gcc",
  "gnumake"
]

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"

